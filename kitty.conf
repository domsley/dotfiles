# Default window padding (in pixels)
window_padding_width 15

# Enable remote control so programs like Neovim can dynamically change Kitty settings
allow_remote_control yes

# Set a fixed Unix socket path for remote control
# This allows Neovim and other tools to find Kitty's control socket reliably
listen_on unix:/tmp/kitty
